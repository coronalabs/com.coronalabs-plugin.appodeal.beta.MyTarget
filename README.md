Sources for the plugin `plugin.appodeal.beta.MyTarget`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.MyTarget" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
